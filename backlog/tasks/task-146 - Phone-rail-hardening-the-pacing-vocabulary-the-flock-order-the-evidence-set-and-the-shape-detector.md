---
id: TASK-146
title: >-
  Phone rail hardening: the pacing vocabulary, the flock order, the evidence set
  and the shape detector
status: Done
assignee: []
created_date: '2026-09-21 11:31'
updated_date: '2026-09-21 11:43'
labels: []
dependencies: []
ordinal: 154000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Two reviewers examined the phone-rail and Luna-grounding work on 2026-09-21 and raised 22 findings. This task is the verify-and-fix pass over them, plus the wiring that turns the rail on.

Three findings were blockers that made the rail carry nothing at all: the VPS client put Luna's action slug in trace.action while the executor's governor paces on that field (every outbound refused 400); the flock was taken after the write-ahead row, so a lock timeout -- the documented normal case on a shared handset -- left an 'attempting' row that answered 504 to that deterministic key forever; and none of WA_TRANSPORT/WA_BRIDGE_* was in the .env either unit reads.

Also fixed here: send_buttons raising a bare NotImplementedError on the only rail Ivan runs, a shape detector that read ordinary German compounds as invented clinics (silencing truthful turns) while missing fabricated names that used no head word, grounding evidence replayed without the listing cut, a tool-call log that swallowed its own write failures while being the sole source of that evidence, town resolution that conflated 'unknown town' with 'nothing live there', clinic doors that counted jobs_open instead of jobs_live, a bubble match that accepted an identical bubble from an earlier turn as proof of this send, a thread read that stamped every visible bubble with today's date, an empty uiautomator dump that looked exactly like a quiet chat, a follow-up nudge with no turn key, an invented cap on narrowing values, get_posting degrading to all-nulls, board URLs in tool rows, and a display name shared by two contacts resolving to whichever came first.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Every action slug Luna produces reaches the handset: trace.action carries the governor's pacing class, the slug rides as trace.intent, and a test feeds bridge.Client's real payload into bridge.executor.Executor.send
- [x] #2 A busy flock is a retryable 503 that leaves no ledger row, so the deterministic key stays usable
- [x] #3 A multi-bubble turn waits out the fuse's published next_slot_at instead of being refused 429 halfway
- [x] #4 send_buttons renders numbered text and never raises an unclassified exception; consent is still tap-only
- [x] #5 The shape detector reads ordinary German compounds as prose and fabricated houses as names, both proven on the sentences from the review
- [x] #6 Grounding evidence is the rows a listing tool showed, and a tool-log write failure raises instead of reading as an invention
- [x] #7 The rail is connected: WA_TRANSPORT, WA_BRIDGE_URL, WA_BRIDGE_PHONE_NUMBER_ID and both secrets in one file read by both units, with the tunnel and relay installed
- [x] #8 A verified bubble match cannot be an identical bubble from an earlier turn, and a thread read never stamps an older bubble with today
- [x] #9 Ivan receives a real WhatsApp reply composed by Luna on the VPS and delivered by our service through the handset
- [x] #10 The offline suite stays green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
VERIFIED LIVE, 2026-09-21 11:35-11:37 UTC. Ivan's inbound wab.i.bd3bf1b0 was pulled off the handset's notification shade by the mini watcher, drained by pflege-wa-bridge-relay through the ssh -L, accepted by POST /api/wa/bridge-webhook (which had answered 404 all morning because the running service predated the route), answered by Luna on the VPS, and delivered as two bubbles by the executor on the phone. Both ledger rows: state=sent, tick=Zugestellt, tick_state=delivered, read off the bubble at send time. Harness: claim state 'sent', thread pinned rail='bridge', 0 wa_inbound_pending, 0 wa_send_failures.

The second bubble went 37 s after the first -- the client waiting out the fuse's published next_slot_at. That case was found by the new cross-side test, not by review: the handset's floor is 4 s between bubbles to one person, so before this every multi-bubble Luna reply lost its second bubble to a 429, failed the whole turn, and was redelivered three minutes later by catch-up with the remaining bubbles regenerated by a second brain call.

The journal shows the two-door id equality holding live: thread_read {seen:1, stored:0} after each send -- the thread door minted exactly the id the shade had already minted for the same message, so nothing was answered twice.

FINDINGS REJECTED, with reasons:
- #19 (offer is null mid-funnel): building the offer before the gates are settled is exactly the per-city preview TASK-91 removed, and luna_brain says so in a comment. The prompt moved instead: the VOLUME rule now names the listing tool's own {shown, total} mid-funnel and market_snapshot.offer at the close.
- #9's suggested fix (release the flock between typing and the tick read): a tick read off a thread the other lane may have scrolled is not evidence. The docstring now states the real 90-150 s budget and names who pays for it, and the watcher counts busy cycles instead of logging errors.
- #5's suggested mechanism (day separators are 'already parsed as RID_DATE siblings'): not true on this handset. A real dump shows 8 'date' nodes for 7 messages, the extra one a voice note's own timestamp, and no divider node at all. The day is derived instead: the read runs right after our own send, so the bottom of the thread is today; bubbles above the lowest divider (a 'date' node whose text is not HH:MM) are reported unresolved.

RESIDUAL, stated not guarded: an invented house whose own words are nowhere in the board vocabulary AND whose town sits outside the name is not corroborated, so detector 2 does not fire on it. Corroborating from the whole sentence would catch it and would bring back the false positive that silences a truthful turn. Pinned by a test.

STILL OPEN AND NAMED: TASK-121's id recovery (a typed '1' is free text for now), TASK-122 synthetic consent (so the funnel's close is not reachable on this rail yet), TASK-125 bridge_sync (nothing resolves an 'attempting' row automatically, which is why every nothing-was-typed refusal now leaves no row), TASK-136 the handset's MSISDN.

.env was NOT touched (it is off limits): the rail's five variables live in ~/.local/state/pflege-wa-bridge/rail.env, mounted on pflege-wa.service through the drop-in /etc/systemd/system/pflege-wa.service.d/10-phone-rail.conf and on the relay unit directly. The stale, unreferenced tokens.env was removed so there is one source.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Verified all 22 review findings against the live tree and the live handset; fixed 19, rejected 3 with reasons, and turned the rail on. Three were blockers that meant the rail could carry nothing: the VPS client put Luna's action slug in trace.action while the executor's governor paces on that field (every outbound refused 400 before reaching the phone); the flock was taken after the write-ahead row, so a lock timeout -- the documented normal case on a handset the other lane also drives -- left an 'attempting' row that answered 504 to that deterministic key forever with nothing to reconcile it; and none of the rail's five variables existed in any file either unit read. A fourth, found by the cross-side test this run adds rather than by review, refused the second bubble of every multi-bubble reply.

Proven by the acceptance test the mission asked for: Ivan's message was pulled off the handset, answered by Luna on the VPS, and delivered as two bubbles by the executor, both carrying a 'Zugestellt' tick read off the bubble at send time. Offline suite 1992 passed / 127 skipped; the mini executor reports ok with a live watcher, 0 errors and 9 busy cycles (the two sends holding the phone, counted rather than logged as incidents).
<!-- SECTION:FINAL_SUMMARY:END -->
