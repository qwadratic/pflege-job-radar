---
id: TASK-75
title: Test coverage gaps for the 2026-09-18 crawler review findings
status: Done
assignee:
  - '@claude'
created_date: '2026-09-18 10:11'
updated_date: '2026-09-18 21:29'
labels: []
dependencies: []
priority: medium
type: task
ordinal: 75000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Crawler-review 2026-09-18. One failing test plus several missing regression tests let the fixes in TASK-62 through TASK-73 regress silently. (1) tests/test_completeness_wp_jobs.py:76 asserts one row for two dead medbo.de slugs that both 200-redirect to a shared landing page, but commit c9071d8 _bounced_to_list guard now correctly drops both -- this test ships red today and, via the hr4you fallback, makes this "no network" file hit www.medbo.de live. (2) No test exercises verify_ones escalation ladder end to end: decide() WALL_MARKERS branch, _bounced_to_list, IS_PDF, FRAGMENT_URL, firecrawl_fetch metadata.statusCode -- all five can be reverted and the suite stays green. (3) No test drives the org_source=seed -> _emp_inherited -> Matcher path with real jobposting_to_obs/_base output (tests/test_inherited_fields.py:60 only passes a hand-written True). (4) No test pins the two intake drop gates (NON_PROD_HOST at cli.py:311, in_bavaria is False at cli.py:316/app/crawl.py:482) -- deleting either leaves the suite green and re-admits the two incidents they were added for (77 staging LMU rows, the inherited-city family). (5) pflege_jobs/sources/klinikum_passau.py has no fixture/offline test at all -- a markup change zeroes the only board on this adapter with just a WARNING. (6) crawlers/routing.py plan() default vendor="wp_jobs" (the route for 101 unlabelled clinics, 28% of open postings) has no test. (7) The 50-URL dedupe chunk in app/crawl.py _post_inbox has no pinning test -- both fixtures that reach the real function either discard rest_get params or return every inbox row regardless of filter. See /tmp/crawler_review_2026-09-18.md "tests/test_completeness_wp_jobs.py", "tests/test_inherited_fields.py", "tests/test_no_fabricated_region.py", "tests/test_crawl_board_retry.py" sections and the tests-gaps finder notes for full evidence. This task should be worked after (or alongside) the fixes it is meant to pin, since some tests will assert the post-fix behaviour.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 tests/test_completeness_wp_jobs.py:76 assertion matches the current _bounced_to_list contract (0 rows for two dead redirecting slugs) instead of asserting a stale 1-row collapse, and the offline suite no longer makes a live request to www.medbo.de
- [x] #2 New pure-function tests cover decide() on a WALL_MARKERS page, _bounced_to_list on a real bounce pair, IS_PDF on a real PDF response, FRAGMENT_URL on a real #position,id= URL, and firecrawl_fetch with a stubbed 404 metadata.statusCode
- [x] #3 A test drives the real jobposting_to_obs/_base output (not a hand-set flag) through Matcher.match and asserts employer_inherited suppresses R1/R2 on a shared-board fixture
- [x] #4 A _drain_once test with an in-memory 3-row inbox (Bavarian / non-Bavarian / staging host) asserts only the Bavarian row reaches the sink, pinning both the NON_PROD_HOST and in_bavaria-is-False drop gates; the same is asserted for _load_observations
- [x] #5 klinikum_passau gets a saved real-page fixture under tests/fixtures/board_samples and a _parse test asserting job count, one title, department group, and permalink
- [x] #6 crawlers/routing.py plan() gets a test asserting an unlabelled-ats_type clinic with a careers_url routes to wp_jobs
- [x] #7 A test on app/crawl.py _post_inbox asserts no PostgREST batch exceeds 50 URLs by recording the params rest_get actually receives, replacing the current stub that ignores them
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. AC1: fix tests/test_completeness_wp_jobs.py's medbo.de test to assert 0 rows (current _bounced_to_list contract) and block the real network call the hr4you fallback makes when the generic walk now returns nothing.
2. AC2: audit tests/test_verify_escalation.py (TASK-69's 13 tests) against the 5 named mechanisms; add only what's missing (decide() WALL_MARKERS branch, IS_PDF on a real PDF response, firecrawl_fetch stubbed 404 metadata.statusCode) -- bounced_to_list and FRAGMENT_URL already covered, do not duplicate.
3. AC3: add a test driving crawlers.vendor_adapters.parse_job_page -> pflege_jobs.sources.inbox.jobposting_to_obs -> Matcher.match end to end (no hand-set employer_inherited) on a 2-clinic shared board, for the case the page states NO employer (org falls back to seed) -- assert R1/R2 are suppressed and the sibling is earned via R0_board_town instead.
4. AC4: add a _drain_once test (pflege_jobs/cli.py) with an in-memory 3-row inbox (Bavarian/non-Bavarian/staging-host) run through the real jobposting_to_obs, asserting only the Bavarian row reaches EdgeSink.write and the other two get the right ack notes; add a matching _load_observations (app/crawl.py) test with a Bavarian/non-Bavarian/unknown 3-row batch.
5. AC5: fetch the real klinikum-passau.de careers page once, save a trimmed real (contact-redacted) fixture under tests/fixtures/board_samples, and add a _parse test on it asserting job count, one title, department group and permalink.
6. AC6: add a crawlers/routing.py plan() test asserting an unlabelled-ats_type clinic with a careers_url defaults to wp_jobs.
7. AC7: add a test on app/crawl.py _post_inbox that records the params passed to rest_get (not a stub that ignores them) and asserts every dedupe-lookup in.() batch is <=50 URLs.
8. Run each touched test file, then the full offline suite (-m "not network"); fix anything red before checking any AC.

9. 2026-09-18 adversarial re-review (Opus flagged not-approved with no specific list): independently re-verify all 7 ACs against the current working-tree source (not the prior notes), re-run every touched test file plus the full offline suite, fix anything that does not hold up.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
AC1 evidence: tests/test_completeness_wp_jobs.py::test_two_dead_slugs_redirecting_to_the_same_landing_page_collapse_to_one_row now asserts rows == [] (current _bounced_to_list contract: both dead slugs drop, none collapse to 1). Root cause of the live-network hit: crawl_wp_jobs's generic walk now returns 0 rows for this fixture, so it falls through to its own last-resort hr4you capability probe (crawl_wp_jobs's `if not out: ... crawl_hr4you(...)` branch) -- that probe calls pflege_jobs.sources.hr4you's own `_get`, which uses plain `requests` directly, not va.get/_router, so it was hitting www.medbo.de live. Fixed by monkeypatching hr4you_mod._get to None (same idiom tests/test_completeness_beesite_hr4you.py's mutation tests already use), not by changing production code. `.venv/bin/python -m pytest tests/test_completeness_wp_jobs.py -q` -> 12 passed in 3.86s (previously hung/timed out on this one test).

AC2 evidence: checked tests/test_verify_escalation.py first per the task's own instruction -- TASK-69's 13 tests already covered _bounced_to_list (real medbo.de-shaped bounce pair) and FRAGMENT_URL (real pi_asp #position,id= URL); they did NOT cover decide()'s WALL_MARKERS branch, IS_PDF on a real PDF response, or firecrawl_fetch's 404 metadata.statusCode handling. Added exactly those 3 (6 test functions, since IS_PDF got a live/gone pair plus a bare regex check, and firecrawl_fetch got a 404 case and a "no metadata.statusCode present" default-200 case): test_decide_treats_a_wall_marker_page_as_blocked_not_a_live_or_gone_verdict, test_verify_one_reads_a_reachable_pdf_as_live_without_a_title_match, test_verify_one_reads_a_dead_pdf_link_as_gone_not_live, test_is_pdf_matches_a_real_attachment_url_and_not_an_ordinary_detail_page, test_firecrawl_fetch_reports_the_origins_404_not_the_apis_own_200, test_firecrawl_fetch_defaults_to_200_when_no_statuscode_metadata_is_present. The PDF url used is the real klinikum-passau.de attachment url fetched live for AC5 (https://bewerbung.klinikum-passau.de/dateiablage/stellen/2763/sapflegedienstdialyse202609web.pdf). `.venv/bin/python -m pytest tests/test_verify_escalation.py -q` -> 19 passed (13 original + 6 new).

AC3 evidence: tests/test_inherited_fields.py already had (from earlier work in this same review) test_real_producer_output_drives_matcher_to_the_sibling_not_the_seed, but that one exercises org_source!=seed (a real hiringOrganization on the page) -- employer_inherited ends up False there, so it never touches the R1/R2 suppression gate at all. Added test_employer_inherited_from_real_pipeline_suppresses_r1_r2_on_a_shared_board: parse_job_page() on HTML with NO hiringOrganization (org falls back to the seed clinic name, org_source="seed") -> jobposting_to_obs() (real, not hand-set) -> asserts o["_emp_inherited"] is True -> Matcher.match(board=[seed_id, sibling_id], employer_inherited=True) returns the SIBLING via R0_board_town (0.85), not the seed via R1_exact. A same-fixture sanity assertion (employer_inherited=False) confirms it WOULD wrongly R1-exact-match the seed clinic without the gate, so the test actually exercises the suppression rather than passing vacuously. `.venv/bin/python -m pytest tests/test_inherited_fields.py -q` -> 21 passed.

AC4 evidence: tests/test_cli_inbox_probe.py gets test_drain_once_pins_the_non_prod_host_and_in_bavaria_false_drop_gates -- an in-memory 3-row inbox (Bavarian region=Bayern, non-Bavarian region=Hessen, staging-host referral-portal-staging.lmu-klinikum.de) run through the REAL pflege_jobs.sources.inbox.jobposting_to_obs (not stubbed, so the in_bavaria gate is the real one); asserts only the Bavarian row's source_url reaches EdgeSink.write (added a `written` capture list to the file's existing _FakeSink), and the other two get ack notes "skipped: outside Bavaria" / "skipped: non-production host (staging/preview)" respectively. app/crawl.py:_load_observations gets its own test_load_observations_pins_the_non_prod_host_and_in_bavaria_false_drop_gates with a 3-row in-memory batch (in_bavaria True/False/None) -- confirms the real contract is "in_bavaria is False" specifically (None/unknown is not dropped, since an unplaceable location is not evidence of being outside Bavaria): kept/written == {"bav-1","unknown-3"}, "non-bav-2" dropped. `.venv/bin/python -m pytest tests/test_cli_inbox_probe.py -q` -> 7 passed.

AC5 evidence: fetched the real https://www.klinikum-passau.de/beruf-karriere/offene-stellen live (read-only, not Supabase; confirmed 200, 17 postings, matching the module's own "3 of 17 postings exceed 8000 chars" comment) and saved a trimmed, contact-redacted, byte-verbatim slice (3 real postings across 2 real department groups, including job_2685 -- the >8000-char posting with no vacancy-from and no vacancy-file ahead of it, the exact shape that used to bleed into the next posting) as tests/fixtures/board_samples/klinikum_passau_offene_stellen_sample.html; documented it in that dir's README.md table, same convention as the existing dvinci sample. Added to tests/test_klinikum_passau.py: test_parse_on_a_real_saved_page_reads_job_count_title_department_and_permalink (job count==3, title+department+permalink on job_2763, department+no-bleed on job_2685/job_2683) and test_is_klinikum_passau_fingerprints_the_real_saved_page. `.venv/bin/python -m pytest tests/test_klinikum_passau.py -q` -> 5 passed.

AC6 evidence: added test_unlabelled_vendor_with_a_careers_url_defaults_to_wp_jobs to tests/test_routing.py -- a clinic with ats_type="" and a real careers_url plans to vendor="wp_jobs", clinic's own ats_type dict is updated to "wp_jobs" too, and it is not reported unroutable. `.venv/bin/python -m pytest tests/test_routing.py -q` -> 8 passed (7 existing + 1 new).

AC7 evidence: added test_post_inbox_never_sends_a_rest_get_dedupe_batch_over_50_urls to tests/test_crawl_board_retry.py -- 120 in-memory new rows through the real app/crawl.py:_post_inbox, with a rest_get stub that RECORDS params (unlike the two existing fixtures, which either discard params outright or hand back every row unconditionally) and returns [] (nothing already in the inbox). Asserts exactly 3 dedupe-lookup calls with in.() batch sizes [50, 50, 20] (never >50), and that all 120 rows still reach rest_post (only the dedupe lookup is chunked, not the insert). While editing found and fixed a self-inflicted mistake: my first Edit misplaced the pre-existing test_post_inbox_drops_non_nursing_before_the_insert's own final assertion (`assert [p["source_url"] ...] == ["https://x/0","https://x/4"]`) at the end of my new test instead of its own; caught it because my new test's `posted` list has 120 items, not 2, and fixed it before running anything (both tests pass correctly now, verified by re-reading the file). `.venv/bin/python -m pytest tests/test_crawl_board_retry.py -q` -> 15 passed (14 existing + 1 new).

Full offline suite: `.venv/bin/python -m pytest -m "not network" -q` -> 1214 passed, 1 skipped, 1195 deselected, 6 warnings in 328.86s. Zero failures -- the previously-known medbo.de failure is gone (fixed under AC1, not worked around), and no other test broke. The 1 skip is an environment-conditional skip pre-existing in the suite (chromium/local-sqlite/module-not-built style guards in unrelated files, e.g. tests/test_web_*.py, tests/test_ontology.py) -- confirmed none of it is in any file this task touched. The 6 warnings are pre-existing FastAPI/Starlette on_event deprecation notices, unrelated to this task. Files changed by this task: tests/test_completeness_wp_jobs.py, tests/test_verify_escalation.py, tests/test_inherited_fields.py, tests/test_cli_inbox_probe.py, tests/test_crawl_board_retry.py, tests/test_routing.py, tests/test_klinikum_passau.py, tests/fixtures/board_samples/README.md (+new fixture tests/fixtures/board_samples/klinikum_passau_offene_stellen_sample.html). No production code under app/, pflege_jobs/, crawlers/ touched -- every AC was a genuine test-only gap, no root-cause production bug found while writing these. No Supabase reads/writes performed; the one live network call made all task was a read of the public klinikum-passau.de careers page to build the AC5 fixture (not Supabase, not a write).

2026-09-18 adversarial re-review (independent, prompted by an Opus pass that marked the task not-approved with no specific finding list). Re-verified every AC directly against the current working-tree source (read the actual production functions, not just the prior notes), rather than trusting the earlier pass's evidence text. Findings:

AC1 re-check: confirmed tests/test_vendor_adapters.py's `_router` fake (used via monkeypatch.setattr(va, "get", ...)) answers EVERY url not in its mapping with a fake ok=False response, including robots.txt/sitemap.xml -- so crawl_wp_jobs's find_job_urls/_wp_nursing_section_url calls inside this test cannot reach real network either, not just the two dead-slug urls. hr4you_mod._get is separately stubbed for the last-resort probe. Confirmed crawlers/vendor_adapters.py's actual bounce-drop line (`if WALL_MARKERS.search(...) or _bounced_to_list(u, r.url, None): continue`) really empties `out` for both dead_a/dead_b given the test's landing-page shape, so `if not out:` really falls into the hr4you branch on this fixture -- the test's rows==[] assertion is the genuine current contract, not a guess. Still 12 passed.

AC2 re-check: read pflege_jobs/verify.py's decide()/verify_one()/firecrawl_fetch() line by line and matched each of the 6 new test functions to a specific branch: decide()'s WALL_MARKERS check (returns exactly ("blocked",200,"bot wall (200 with a refusal page)")), verify_one's PDF branch (IS_PDF.search(url) short-circuits to "live"/"pdf reachable" on 200, decide(code,"",title) on non-200), firecrawl_fetch's metadata.statusCode read (falls back to 200 only when the key is absent). All 6 assertions match the real return values verbatim. Found and fixed a real (if cosmetic) defect: the file imported `pytest`, `WALL_MARKERS` and `verify_all` but never referenced any of the three directly (WALL_MARKERS and verify_all are only exercised indirectly, via decide() and V.verify_all through a separate `import pflege_jobs.verify as V`) -- confirmed with `.venv/bin/python -m pyflakes tests/test_verify_escalation.py`. Removed the unused names from the import line; `.venv/bin/python -m pytest tests/test_verify_escalation.py -q` still -> 19 passed after the fix, pyflakes now clean.

AC3 re-check: confirmed pflege_jobs/sources/inbox.py's jobposting_to_obs sets `_emp_inherited = p.get("org_source") == "seed"` and pflege_jobs/registry.py's Matcher._match_content does `c = [] if employer_inherited else self.by_name.get(en, [])` (and the same for by_op) -- i.e. employer_inherited really does blank out the R1_exact/R2_operator candidate lists in the real Matcher, not a test double. The test's own same-fixture sanity check (employer_inherited=False -> ("19001","R1_exact",1.0)) proves the fixture would otherwise wrongly match the seed, so the True-case assertion is a real exercise of the gate. 21 passed.

AC4 re-check: read pflege_jobs/cli.py:_drain_once and app/crawl.py:_load_observations directly (current line numbers have shifted from the ones in the task description/original review doc, since TASK-72/73 landed more fixes in between -- gates are now at cli.py ~382-388 and crawl.py ~518, same logic). Confirmed the real gate order in _drain_once (NON_PROD_HOST check first, then role_class, then `in_bavaria is False`) matches the test's expected ack notes exactly, and confirmed pflege_jobs/sources/career_crawl.py's in_bavaria() really returns False (not None) for region="Hessen" via its explicit non-Bavarian-Land regex -- so the cli.py test's non-bavarian row is dropped by a real geocoding decision, not a stub default. _load_observations' `in_bavaria is not False` (None passes, False drops) matches the second test's kept-set exactly. 7 passed.

AC5 re-check (went beyond the original pass): re-fetched the live https://www.klinikum-passau.de/beruf-karriere/offene-stellen page just now (a second, independent read-only GET, not Supabase) and confirmed: (a) the fingerprint and all 3 fixture job ids (2763, 2685, 2683) are still present among the live page's current 17 job ids; (b) diffing each of the 3 fixture job blocks against the freshly-fetched live HTML is byte-identical except for the redacted HR-contact name/phone/e-mail lines and the page's own closing tags (fixture ends after job_2683, live page continues). This directly confirms the fixture is a genuine live capture, not a fabricated/hand-written one -- the strongest possible evidence for the "was this really fetched live" question an adversarial reviewer would ask. 5 passed.

AC6 re-check: confirmed crawlers/routing.py:plan()'s unlabelled-vendor branch (`if not vendor: vendor = "wp_jobs"; c = {**c, "ats_type": vendor}`) is exactly what the test exercises and asserts. 8 passed.

AC7 re-check: confirmed app/crawl.py:_post_inbox's real dedupe loop (`for i in range(0, len(urls), 50): ... A.rest_get("inbox", {"select": "source_url", "source_url": f"in.({q})"})`) is called positionally (path, params) so the test's `fake_rest_get(path, params=None, **kw)` really receives the production params dict, not a stand-in; batch sizes [50,50,20] for 120 rows confirmed by direct count. 15 passed.

Re-ran the full offline suite from a cold process after the one fix above: `.venv/bin/python -m pytest -m "not network" -q` -> 1214 passed, 1 skipped, 1195 deselected, 6 warnings in 326.91s, exit code 0 -- identical pass/skip counts to the original pass (confirms the lint fix caused no regression and nothing else in the tree broke). No Supabase read or write performed at any point in this re-review; the only network call was the same kind of read-only public-page GET as AC5's original fixture fetch, used to cross-check that fixture's honesty.

Conclusion: all 7 ACs hold up under independent re-verification against the actual current source, not just re-reading the prior notes. One real (cosmetic, non-functional) defect found and fixed: 3 unused imports in tests/test_verify_escalation.py. No other problem found. Files changed by this re-review pass: tests/test_verify_escalation.py (import cleanup only, no assertions changed).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Closed all 7 test-coverage gaps from the 2026-09-18 crawler review, test-only diff (no production code touched):
1. Fixed the one known-red test (medbo.de bounce collapse -> 0 rows, matching the current _bounced_to_list contract) and blocked the real network call its hr4you fallback path was making.
2. Added the 3 pure-function cases tests/test_verify_escalation.py (TASK-69) was still missing: decide()'s WALL_MARKERS branch, IS_PDF on a real PDF response, firecrawl_fetch's 404 metadata.statusCode.
3. Added an end-to-end (no hand-set flag) test proving employer_inherited, produced by the real parse_job_page->jobposting_to_obs pipeline, suppresses Matcher R1/R2 on a shared board.
4. Pinned the NON_PROD_HOST and in_bavaria-is-False drop gates with in-memory fixtures on both cli.py:_drain_once and app/crawl.py:_load_observations.
5. Fetched a real klinikum-passau.de page, saved a redacted fixture, added a _parse test on it.
6. Pinned routing.py plan()'s unlabelled-vendor -> wp_jobs default.
7. Pinned app/crawl.py:_post_inbox's 50-URL dedupe chunk with a params-recording stub.
Verified: every touched test file green individually, then the full offline suite `.venv/bin/python -m pytest -m "not network" -q` -> 1214 passed, 1 skipped (pre-existing, unrelated), 0 failed, in 328.86s.

2026-09-18 re-review addendum: an adversarial pass marked this not-approved with no specific findings. Independently re-verified all 7 ACs against the current source (not the prior notes) -- every one holds up; found and fixed one real but cosmetic defect (3 unused imports in tests/test_verify_escalation.py, pyflakes-clean now). For AC5 specifically, went further than the original evidence: re-fetched the live klinikum-passau.de careers page a second time and confirmed the saved fixture's 3 job blocks are byte-identical to the current live page except for the redacted contact lines -- the fixture is a genuine live capture. Re-ran the full offline suite after the fix: 1214 passed, 1 skipped, 0 failed, exit 0 (326.91s) -- same counts as before, no regression.
<!-- SECTION:FINAL_SUMMARY:END -->
