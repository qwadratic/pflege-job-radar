raw_count 53

SUMMARY: 33 raw findings -> 33 ranked, after merges and one drop. Verified in-repo (not just re-read): app/auth.py:322-325 prefix lists, app/main.py:59-87/200-300, app/crawl.py:120-240/500-535/590-615, app/data.py:254-259, app/autopilot/api.py:95-145/385-400, app/runs.py:144-153, app/mechanics.py:71.

Merges: autopilot auth gap + its docs absence (consistency + docs-drift) = one finding; /api/stats credit leak + public /api/crawl/plan = one "the owner read gate is defeated by two public siblings"; firecrawl.enabled + campaign.stopped = one "documented kill switches read by nobody" (both fix in kill_switch()); the three auth-doc contradictions (docs/api.md "no auth for reads", docs/auth.md matrix, skill's X-ExeDev-Email vs the real X-Api-Key) = one; missing-endpoint drift from three audits = one; field-level drift on 8 endpoints (settings/clinics/facets/search/stats/mechanics/crawl/crawl-plan + filter and sort lists) = one; the bad-limit 500 and the RequestValidationError `detail` split = one error-contract finding; max_credits default/bounds mismatch + MIN_VIABLE_CAP not enforced at the boundary = one; billToday over-fetch + sequential boot chain = one.

Dropped: the "add format=csv bulk export to /api/jobs" item — ~1.2k open rows fit under the existing 2000 clamp, no consumer asks for it, and PostgREST already serves that use case; the clamp itself is now folded into the pagination-envelope finding. Also dropped the proposed verify_status history table (new table + 2 endpoints for a question nobody has asked yet); the cheap half of it — expose verify_http/verify_note in JOB_COLS — survives inside the /api/jobs docs finding.

Severity note worth carrying to whoever acts: the autopilot data is currently seeded synthetic (example.org addresses), so the PII exposure there is latent while the unauthenticated destructive writes (seed/reset, policy, tick) are live today.

1. [high] ALL /api/autopilot/* (app/autopilot/api.py, mounted app/main.py:35)
   problem: The autopilot router is absent from both prefix lists in app/auth.py:322/324, while GATED_PAGES (app/auth.py:325) gates the /autopilot page at member. So the shell is gated and all ~48 routes behind it are open to anonymous callers: GET /api/autopilot/candidates returns rows built as dict(cand, phone_masked=...) (app/autopilot/api.py:397) i.e. raw phone AND email next to the mask, and destructive writes POST /api/autopilot/seed/reset (:1019, truncate+reseed), PUT /api/autopilot/policy (:862), POST /api/autopilot/tick (:883) have no gate either. Neither doc of record mentions /api/autopilot at all, and skill/references/api.md:3 asserts 'all writes are owner-only', which is false here.
   fix: Add "/api/autopilot" to OWNER_READ_PREFIXES and OWNER_WRITE_PREFIXES in app/auth.py:322-324 (_prefixed already matches subpaths). Drop the raw phone/email columns from the list rows at app/autopilot/api.py:397 and :102-107 -- phone_masked already exists. Then document the router (or explicitly document it as open PoC) in docs/api.md + skill/references/api.md. Note: today's rows are seeded synthetic data (example.org), so the PII exposure is latent, not live -- the destructive writes are live now.

2. [high] POST /api/autocrawl/tick (app/main.py:290)
   problem: Fires every enabled schedule immediately via S.tick(force=True), queueing Firecrawl runs that spend credits. /api/autocrawl is not in OWNER_WRITE_PREFIXES (app/auth.py:322) so the route is anonymous, while the narrower sibling POST /api/schedules/{sid}/run-now IS gated by the /api/schedules prefix. force=True also deliberately bypasses the scheduler pause that the kill switch sets (app/scheduler.py:34-39).
   fix: Add "/api/autocrawl" to OWNER_WRITE_PREFIXES in app/auth.py:322 only (not AGENT_WRITE_PREFIXES -- the comment at app/auth.py:327-331 keeps spend controls owner-login-only).

3. [high] GET /api/stats, GET /api/crawl/plan (vs gated GET /api/firecrawl/credits, /api/billing)
   problem: Two public endpoints re-serve the exact data the owner gate protects. app/main.py:76-87 inlines the whole FA.credits() dict plus spent_by_app / spent_by_app_7d into public /api/stats -- the same payload /api/firecrawl/credits (app/main.py:446) 401s on. And OWNER_READ_PREFIXES lists "/api/crawl/runs" but not "/api/crawl" (app/auth.py:324), so GET /api/crawl/plan is public and returns credits_left / est_credits / credits_needed plus the full routing decision per clinic_id (app/main.py:237-241). The gate entries for /api/firecrawl/credits and /api/billing are therefore ineffective.
   fix: Widen the read gate to "/api/crawl" in app/auth.py:324 (strict superset of the existing /api/crawl/runs entry), and in app/main.py:76-87 emit the firecrawl block only when request.state.identity is owner -- otherwise trim to the non-sensitive subset skill/references/api.md:7 documents. If the balance is genuinely public, delete "/api/firecrawl/credits" from OWNER_READ_PREFIXES instead so the two agree.

4. [high] POST /api/crawl (app/main.py:217-220)
   problem: Both spend guards test mode == "firecrawl", but plan_for() routes every non-routable/walled clinic to Firecrawl under mode="auto" too (app/crawl.py:231). So {"scope":"all","mode":"auto"} skips both the 400 'refusing firecrawl for every hospital at once' and the 409 weekly-budget check while queueing the same ~58 Firecrawl clinics. Only the per-clinic _budget_left() re-check inside execute() (app/crawl.py:514) bounds the damage, and it surfaces as a half-failed run rather than a refused request.
   fix: Key both guards on the plan, not the mode, in app/main.py:217-220: `if plan["firecrawl"] and target["scope"]=="all": raise HTTPException(400, ...)` and `if plan["firecrawl"] and plan["credits_needed"] > plan["credits_left"]: raise HTTPException(409, ...)`.

5. [high] PUT /api/settings/firecrawl {"enabled":false}, POST /api/campaign (stopped/stop_reason)
   problem: Two documented kill switches are read by nobody on the crawl path. firecrawl.enabled is documented in app/settings.py:22 as 'false = no Firecrawl call at all' but its only reader is app/hunter.py:397 -- kill_switch() (app/crawl.py:79), spend_gate(), execute() and refetch_career() never consult it, so POST /api/crawl, the scheduler and the campaign keep spending after the master switch is off. Likewise campaign stopped/stop_reason (app/campaign.py:11-16) is write-only state: grep over app/ shows no reader outside app/campaign.py, so a declared hard stop is honour-system and any schedule tick or a human clicking Run keeps burning credits.
   fix: Both belong at the one choke point every Firecrawl path already crosses -- the first lines of kill_switch() in app/crawl.py:79: return (False, "firecrawl.enabled is false") when ST.get_firecrawl().get("enabled", True) is False, and (False, f"campaign stopped: {reason}") when CAM.get()["stopped"]. Then delete the duplicate check at app/hunter.py:397 and surface both as the plan preview's blocked reason.

6. [high] POST /api/crawl (mode=firecrawl) -- app/crawl.py:130 _adapter_probe_rows / :186 spend_gate
   problem: For a routable, non-walled clinic spend_gate() runs the real adapter (_vendor_rows / _seed_obs -- the same functions execute() ingests with) and keeps only the URL strings, discarding the rows. When there are unseen rows the gate says allowed and execute() then pays for a Firecrawl agent run to re-discover postings the free probe held in memory seconds earlier. docs/campaign.md drives the reingest campaign through mode=firecrawl over every clinic, so this is the campaign's main path; the discarded rows also never reach n_new, inflating cost_per_posting for the hunter's combined_bar stop rule.
   fix: Have _adapter_probe_rows() return the rows/observations and spend_gate() pass them back in its result dict; in app/crawl.py:519-524 append gate rows to inbox_rows/observations, and skip the Firecrawl call entirely when the probe already produced the unseen rows.

7. [high] GET /api/crawl/plan (app/main.py:237 -> app/crawl.py:216 plan_for)
   problem: The preview runs none of the gates the run hits. plan_for() only does routing plus credits_needed = len(firecrawl) * max_credits and credits_left = weekly_budget - usage_total(7d). The real decisions live in spend_gate() (app/crawl.py:148) and kill_switch() (app/crawl.py:79) and only run inside execute() (app/crawl.py:506-524): free daily allowance, reserve_credits floor against the account's actual remaining, MIN_VIABLE_CAP=40, max_eur_unknown_clinic, 'adapter covers it', the 24h kill switch. web/pro.template.html:716 even claims the opposite ('reuses spend_gate() logic already behind GET /api/crawl/plan') and enables Confirm on that belief. Result: with remaining=26 / reserve=150 the panel shows 'est 160, 100 left' and an enabled Confirm for a run that charges 0 and ends failed with N 'refused by spend gate' lines; with free runs left it shows 160 credits for a free run.
   fix: Have plan_for() call the real gates: add kill_switch_status() as a top-level blocked/reason, and per Firecrawl clinic call the side-effect-free spend_gate(c, max_credits) to return [{clinic_id, allowed, cap, reason, billable}] plus est_credits_billable / est_credits_free / est_usd. Put the adapter-probe part behind ?gate=1 if live probes make the default path slow, and make showPlanGate() disable Confirm on blocked.

8. [high] docs/api.md, docs/auth.md, skill/references/api.md (auth claims for all owner-gated routes + agent writes)
   problem: Every auth statement in the docs of record is wrong in a different direction. docs/api.md's header says 'no auth for reads' and its Billing section says 'No auth.', but OWNER_READ_PREFIXES gates 8 read prefixes (/api/billing, /api/hunter, /api/settings, /api/coverage, /api/inbox, /api/firecrawl/credits, /api/crawl/runs, /api/campaign) -- GET /api/billing 401s. docs/auth.md's matrix puts /api/crawl/runs in the 'everyone' row (it is owner-only) and omits /api/firecrawl/credits and /api/campaign from the owner row. skill/references/api.md tells agents to send X-ExeDev-Email, which only works when the exe.dev proxy injects it for a logged-in owner, and never mentions X-Api-Key -- the door that actually exists for agents, scoped to AGENT_WRITE_PREFIXES (POST /api/crawl, POST /api/inbox/drain, refetch-career) -- nor PUT|DELETE /api/settings/agent-key / POST /api/auth/agent.
   fix: Make the three docs quote app/auth.py:322-331 verbatim: list OWNER_READ_PREFIXES / OWNER_WRITE_PREFIXES, fix docs/auth.md's row placement, delete 'no auth for reads' and 'No auth.', and state in skill/references/api.md that agents use `X-Api-Key: <key>` valid only on those three routes while X-ExeDev-Email is proxy-injected (browser/owner only). Regenerate web/skill/ afterwards.

9. [high] docs/api.md + skill/references/api.md endpoint tables (~25 non-autopilot routes)
   problem: Diffing /api/openapi.json (106 paths) against both docs of record: 69 paths appear in neither. Excluding autopilot (reported separately), the undocumented set includes the operational and spend-control surface both SPAs already call: GET /api/hunter/status|targets, POST /api/hunter/start|stop|run-once, POST /api/scheduler/pause|resume, GET /api/inbox, POST /api/inbox/drain, GET /api/firecrawl/credits, POST /api/autocrawl/tick, POST /api/refresh-cache, GET /api/schedules/presets, GET /api/billing/clinics, GET /api/me, GET /api/flags, POST /api/auth/magic|logout|agent, GET /api/stripe/status, POST /api/stripe/checkout, GET|POST /api/postings/{id}/closed, PUT /api/settings/firecrawl|flags|hunter, POST /api/settings/patterns/validate. skill/references/api.md names /billing, /coverage, /campaign, /hunter, /inbox in its auth prose but gives them no table row, so an agent learns they need auth and never learns their shape. GET /api/billing/clinics -- the source of the whole Kosten-je-Klinik table -- is in no doc file at all.
   fix: Add an 'Operations (owner only)' block (hunter, scheduler, inbox, firecrawl/credits, autocrawl/tick, refresh-cache, billing, billing/clinics, campaign, coverage, schedules/presets, settings writes) and an 'Identity & billing' block (/me, /flags, /auth/*, /stripe/*, /postings/{id}/closed, /health) to the docs/api.md table, mirror the owner-only ones into skill/references/api.md with response key lists, plus a short 'cost model' paragraph naming the gate order (firecrawl.enabled -> kill switch -> weekly budget -> reserve_credits -> MIN_VIABLE_CAP -> max_eur_unknown_clinic -> adapter-covers-it) and the 400/404/409 codes. Regenerate web/skill/.

10. [high] GET /api/crawl/runs (app/main.py:244 -> app/runs.py:144 list_runs)
   problem: list_runs is a bare `select * from crawl_runs order by run_id desc limit ?` -- no filter on status, mode, trigger, scope, clinic_id or time window, and no search over run_log (only a 3-line log_tail; full lines need one GET /api/crawl/runs/{id} per run). Outcomes that matter live only as log text: a spend-gate refusal is logged at app/crawl.py:522 while the run row stays status=done/error=null. app/hunter.py:79-93 has to regex its own run log for 'credits charged' / 'spend gate' / 'refused' because no structured field exists, and docs/campaign.md:51 says the campaign routine reads journalctl on the VM because the API cannot answer it.
   fix: Add status/mode/trigger/scope/clinic_id/since/until and q (substring over run_log via `exists (select 1 from run_log l where l.run_id=crawl_runs.run_id and l.line like ?)`) to list_runs, returning matching log lines instead of the blind tail (~20 lines in app/runs.py + params in app/main.py). Second, count gate refusals per run in app/crawl.py:520 and store them on the run row so `status=done, refused=11` is queryable.

11. [high] GET /api/clinics (last_crawl_* fields, app/data.py filter_clinics + app/runs.py last_run_per_clinic)
   problem: No way to ask which clinics have never been crawled. filter_clinics exposes no filter on last_crawl_at/status/mode -- only sort=last_crawl_at, which puts nulls last in both directions. The underlying data is also weak: last_run_per_clinic() reads only the newest 500 runs and keeps the most recent run per clinic regardless of outcome, so a clinic whose last run failed is indistinguishable from one never crawled, a success followed by a failure reports failure, and once older runs age out a crawled clinic silently reports last_crawl_at=null. Per-clinic success is not recorded for multi-clinic runs (n_rows/n_new are per run), and /api/coverage returns only per-adapter aggregates. skill/SKILL.md rule 7 obliges every external agent to flag fetch=firecrawl clinics with no Firecrawl run yet -- a claim the API gives it no way to check.
   fix: Group over the whole crawl_runs table (69 rows locally) instead of a 500-row window and expose last_crawl_at, last_success_at, last_firecrawl_at on the clinic row, plus a `crawled=never|failed|ok` filter in app/data.py:filter_clinics modelled on the existing `routable` branch (~10 lines). Document never = no run row ever included this clinic.

12. [medium] GET /api/clinics?has_jobs=0 (app/data.py:254)
   problem: has_jobs is one-way: only `if p.get("has_jobs") in ("1","true")` is handled, so has_jobs=0/false falls through as a no-op and the endpoint returns all ~407 clinics with an authoritative-looking total. The `routable` filter three lines below (app/data.py:256-259) handles both directions. skill/SKILL.md:76 documents the filter as '1/0' and skill/SKILL.md:117 ships the recipe `curl "$B/clinics?size=L,XL&traegerart=oeffentlich&has_jobs=0"  # big public sites without visible jobs` -- the one lead-list query an agent is explicitly told it can run, and it answers a different question with no error.
   fix: Mirror the routable branch: `elif p.get("has_jobs") in ("0","false"): rows = [c for c in rows if not c["jobs_open"]]` in app/data.py:filter_clinics.

13. [medium] POST /api/mechanics/{mid}/test, POST /api/mechanics/{mid}/try (app/main.py:358-379)
   problem: /api/mechanics is not in OWNER_WRITE_PREFIXES (app/auth.py:322), so both POSTs are anonymous. /test spawns subprocess.run([PYTHON, '-m', 'pytest', ...], timeout=120) on every call (app/mechanics.py:71); /try executes registry code from caller-supplied inputs. It is the only unauthenticated POST in the app that forks a subprocess -- a handful of parallel requests starves the process serving the job board.
   fix: Add "/api/mechanics" to OWNER_WRITE_PREFIXES in app/auth.py:322. required_role only checks write methods, so GET /api/mechanics stays public and the docs page is unaffected. Update docs/api.md:25 and skill/SKILL.md:97, which present both POSTs as open.

14. [medium] GET /api/coverage + /api/hunter/status + /api/settings + /api/firecrawl/credits (web/pro.template.html #/clawl)
   problem: Opening #/clawl (pageScrape l.730-830 + crawlStrip l.1059-1074) fires 12 requests against owner-only endpoints, 3 of them literal duplicates: /api/settings is requested by boot (l.1168), by pageScrape (l.730, `SET=SET||await api(...)` racing the un-awaited boot call) and again unconditionally by crawlStrip (l.1062); /api/hunter/status by loadHunter (l.761) and again by crawlStrip. The Firecrawl balance arrives three ways at three refresh rates -- /api/stats.firecrawl (60s), /api/firecrawl/credits (30s poll), hunter/status.pools -- so the same screen can show three different numbers, and /api/firecrawl/credits is byte-for-byte what /api/stats already embeds.
   fix: Free fix: pass the already-fetched data in -- crawlStrip({settings: SET, hunter: hunterData}) and drop its own two calls; drive the credit chip from STATS.firecrawl instead of /api/firecrawl/credits. If round trips still hurt, add one owner-only GET /api/clawl in app/coverage.py returning {coverage, hunter, schedules, inbox, runs, settings.firecrawl, billing_clinics_7d} by calling the existing in-process functions.

15. [medium] GET /api/clinics?limit=1000&sort=name (web/pro.template.html:691 clinicOptions)
   problem: The target-form dropdown pulls the whole clinic table to build {v:clinic_id, label:name·town}. app/data.py:97-104 attaches career_profile (a full discovery blob), fachrichtungen[], routing and last-crawl fields to every row and filter_clinics applies no projection, so ~400 fat objects cross the wire for two strings. pageScrape awaits targetForm() (l.731) which awaits clinicOptions(), so the entire Clawl page blocks on the largest response on the page.
   fix: Add a `fields=clinic_id,name,town` projection to GET /api/clinics (one dict comprehension in _page), or expose clinics:[{v,label}] in GET /api/facets which the page already fetches -- then targetForm needs no second call. Either way stop awaiting clinicOptions() before first paint: render the form and fill the multiSelect when it resolves.

16. [medium] GET /api/clinics, /api/plan, /api/jobs (bad limit) + all typed routes (validation errors)
   problem: The error contract is three-way split. GET /api/clinics?limit=abc -> 500 {"error":"ValueError: invalid literal for int()..."} because untyped dict(request.query_params) reaches int() in _page (app/main.py:69-72) and falls into the _any_err catch-all (app/main.py:64-66), which echoes raw Python exception text to anonymous callers; GET /api/search?q=x&limit=notanint -> 422 {"detail":[...]}; GET /api/autopilot/queue?limit=abc -> 400 {"error":...}. Separately FastAPI's RequestValidationError bypasses the {"error":...} envelope entirely (GET /api/jobs/abc, POST /api/auth/magic {} -> {"detail":[...]}), which both SPAs already work around with `body.error || JSON.stringify(body.detail)` (web/pro.template.html:369, web/autopilot.template.html:293) while skill/references/api.md:3 documents only {"error":...}.
   fix: Wrap the two int() calls in _page (app/main.py:69-72) and in app/autopilot/api.py:58-61 to raise HTTPException(422, "limit and offset must be integers"); register @app.exception_handler(RequestValidationError) returning JSONResponse({"error":"invalid request","detail":exc.errors()}, 422); and make _any_err log the exception but return a fixed {"error":"internal error"}. Then state in both docs that every non-2xx body carries `error`.

17. [medium] GET /api/billing, GET /api/billing/clinics (web/pro.template.html:975 CPP_BAR)
   problem: The SPA hardcodes CPP_BAR=0.5 as the cost-per-posting threshold colouring the billing tile (l.1049) and every clinic row meter (l.1026-1028), but the real bar is hunter.max_usd_per_posting (app/settings.py:32, writable via PUT /api/settings/hunter, enforced at app/hunter.py:363) and the reingest campaign runs at $0.10. Both billing endpoints return cost_per_posting_usd but not the bar it is judged against; the Clawl hunter card gets it right only because /api/hunter/status ships rules.max_usd_per_posting.
   fix: Add max_usd_per_posting from ST.get_hunter() to the totals of app/billing.py report() and app/coverage.py billing_clinics(), read it in normBilling, and keep 0.5 only as the null fallback.

18. [medium] POST /api/clinics/{clinic_id}/refetch-career (app/main.py:277, app/crawl.py:594)
   problem: refetch_career() calls the same spend_gate(c, max_credits) used for jobs runs, so for a routable non-walled clinic the gate runs the jobs adapter probe (app/crawl.py:179-192) and can return allowed=False 'adapter covers it' -- a verdict about job rows applied to a career-page discovery run that uses a different agent (run_career_agent) and a different ledger kind ('career'). The operator gets a nonsense refusal with no way to proceed, after paying the wall-clock cost of a discarded live adapter fetch. The endpoint also has no plan/preview and no bound on max_credits (POST /api/crawl enforces 0..500 at app/main.py:209; here any int is accepted).
   fix: Give spend_gate() a kind='jobs'|'career' argument and skip the coverage-probe branch (app/crawl.py:179-192) for kind='career', keeping the reserve floor, MIN_VIABLE_CAP and the EUR cap. Apply the same 0..500 validation as POST /api/crawl.

19. [medium] GET /api/firecrawl/credits (app/main.py:446)
   problem: kill_switch_status() (app/crawl.py:56) already computes pct-of-plan-in-24h, the [warn, throttle, disable] thresholds, used_24h and free runs left, and is exposed by no endpoint -- only kill_switch_pct() uses it inside the run path. /api/firecrawl/credits returns pools plus spent_by_app / spent_by_app_7d (all-time / 7d), never the 24h number the switch actually decides on. A client cannot learn that the next run will be throttled or disabled, or that the switch auto-paused the scheduler (app/crawl.py:86-90), until a run finishes failed with the reason in a log line; reconstructing it needs three endpoints and is still an approximation.
   fix: One line at app/main.py:446: fc["kill_switch"] = CR.kill_switch_status(). Then the header credits pill and the plan panel can show throttled/disabled with no extra call.

20. [medium] GET /api/billing (per-run cost attribution, app/billing.py:281-321)
   problem: No way to ask why a run cost what it cost. runs[] carries credits, usd, free and clinic_ids[0] only -- the per-clinic firecrawl_usage rows (clinic_id/job_id/tokens) are collapsed at app/billing.py:281-288, so multi-clinic runs lose attribution entirely; the cap actually used (gate['cap'], which differs from params.max_credits after reserve/EUR caps), the per-clinic gate verdict, and zero-cost refusals exist only as free text in run_log. firecrawl_events (verbatim per-job creditsUsed from the webhook, app/runs.py:34) has no endpoint at all. app/hunter.py:79-90 proves the cost: it regex-greps its own run log because no structured field exists.
   fix: Add GET /api/billing?run_id= (or /api/billing/runs/{run_id}) returning the run row + its firecrawl_usage rows + params.max_credits + matching firecrawl_events -- one query per table, all data already in SQLite. Cheaper first step: stop collapsing lrows and emit a clinics[] breakdown inside each runs[] entry.

21. [medium] POST /api/hunter/start, POST /api/hunter/run-once (app/hunter_api.py:32, :64)
   problem: Both unconditionally clear today's stop reason (H.day_set(H.today(), 'stop_reason', None)) and return stop_reason: None, so a cost stop such as 'combined_bar: refills 2 >= 2 AND cost per posting $0.63 > $0.50' or 'suspicious: burn rate 900 credits/hour' is erased without ever being shown to the caller -- the hunter's stop rules are the account's spend backstop ('wait for a human', app/hunter.py:15-19). There is also no preview: Hunter.dry_run() (app/hunter.py:280) produces exactly the 'what would run, at what cap' list but is reachable only via `python -m app.hunter --dry-run`; GET /api/hunter/targets returns already-decided rows for today, not pending candidates.
   fix: Return {"cleared_stop_reason": <old>} from start/run-once and require ?force=1 to clear a combined_bar:/suspicious: stop (plain 409 with the reason otherwise). Expose the existing preview as GET /api/hunter/dry-run -- a two-line router entry over H.Hunter(...).dry_run(), which already returns targets, caps, verdicts and rules.

22. [medium] GET /api/crawl/runs, /api/schedules, /api/mechanics, /api/cities, /api/hunter/targets, /api/billing/clinics (list envelope)
   problem: docs/api.md:32 states flatly 'List responses: {"total": N, "rows": [...]}'. Two things are wrong: _page actually returns {total, limit, offset, rows} (app/main.py:71), and the envelope only applies to /api/clinics, /api/plan, /api/jobs and the autopilot routes. /api/crawl/runs (app/main.py:244, despite taking limit), /api/schedules (:297), /api/mechanics (:345) and /api/cities (:107) return bare arrays; /api/hunter/targets returns {day, rows} with no total; /api/billing/clinics (app/coverage.py:342) takes limit but no offset and returns rows[:limit] with no signal that it truncated the cost table. Per-endpoint default page sizes (clinics 500, jobs 200, plan 1000, cap 2000) are undocumented, and the 2000 clamp is silent.
   fix: Fix the doc first: in docs/api.md:32 and skill/references/api.md name the exact endpoints using {total, limit, offset, rows}, note the rest return bare arrays, and document the per-endpoint defaults and the silent 2000 clamp. If converging one endpoint, make it /api/billing/clinics -- it already truncates, so add offset and total to the return dict at app/coverage.py:338-339.

23. [medium] GET /api/jobs (documented row fields + status filter)
   problem: docs/api.md's 'Job row' list includes source_url and skill/references/api.md says 'v_postings columns + fresh', but JOB_COLS (app/data.py:16) omits source_url, is_pflege, offer_kind, salary_min/max/unit, in_bavaria, verify_http, verify_note and more, while adding three undocumented ones (clinic_town, clinic_size, employer_id); source_url exists only on GET /api/jobs/{id} (app/data.py:327). Separately skill/references/api.md documents `status: open | expired` as a field and enum, but the snapshot query is fixed to status=eq.open (app/data.py:79) and filter_jobs has no status key, so ?status=expired is silently dropped and returns open rows with a 200.
   fix: Replace both docs' job-row claims with the literal JOB_COLS list + fresh, note 'source_url only on GET /api/jobs/{id}', and add on the /jobs row: 'open postings only; status is always open -- use PostgREST v_postings?status=eq.expired'. Add verify_http/verify_note to JOB_COLS so the current verify reason is visible in list rows.

24. [medium] GET /api/settings, /api/clinics, /api/facets, /api/search, /api/stats, /api/mechanics, /api/crawl, /api/crawl/plan (documented field lists)
   problem: Field-level drift across eight endpoints. /settings is documented as {patterns, firecrawl{default_max_credits}} but ST.get_all() (app/settings.py:132) returns patterns, patterns_path, scheduler, firecrawl, hunter, feature_flags, feature_flags_info, feature_status_notes, agent_key. The clinic-row list in both docs omits board, board_shared, vendor, size, plz, parse_quality, source, updated_at, employer_id -- including the routing facts and the `size` bucket the documented size=L,XL filter matches on. /facets live adds job_cities (the job-city facet; the documented `cities` is clinic towns), verify_status, size. /search adds role_class, fresh, ats_type, jobs_open, regierungsbezirk. /stats adds clinics_active, clinics_with_ats, active_runs, snapshot_at, free_runs_left_today, token counters. Mechanics rows are documented with a `stage` field ME.describe() never emits. POST /api/crawl actually returns {run_id, queued, clinics, boards, adapter, firecrawl, skipped[]} and GET /api/crawl/plan adds target, credits_left, skipped[], adapter[ids], firecrawl[ids], credits_needed plus an undocumented max_credits param. Filter lists are short too: /jobs also accepts size, enr_tariff, versorgungsstufe, traegerart, qualification_hint; /clinics also accepts routable=1|0; and `sort` is whitelisted -- an unknown key is silently replaced (jobs -> first_published, clinics -> jobs_open) with no error and the allowed keys listed nowhere.
   fix: Regenerate these rows in skill/references/api.md and docs/api.md from live responses (curl /api/settings /api/facets /api/search?q=x /api/stats /api/clinics /api/crawl/plan), call out cities vs job_cities in one clause, drop `stage` from the mechanics row, add the missing filters, and add one line: 'sort must be one of <list>; anything else falls back to the default sort'. Regenerate web/skill/.

25. [medium] GET /api/clinics (field size) / GET /api/facets (size_buckets)
   problem: web/pro.template.html:360 hardcodes `sizeOf=b=>b<100?"S":b<300?"M":b<800?"L":"XL"` and l.663 hardcodes ["S","M","L","XL"], while the API already returns `size` on every clinic row (app/data.py:97, driven by taxonomy.json size_buckets) and ships the bucket definitions with labels in facets.size_buckets (app/data.py:156). The clinic table (l.520) and detail page (l.564) both call sizeOf(c.beds) and ignore c.size, so editing size_buckets changes what ?size=L returns and what the facet counts say while the UI chips keep the old thresholds -- filter and label disagree with no error.
   fix: Use c.size at both call sites, build the search-suggestion list from FACETS.size_buckets, delete sizeOf.

26. [low] GET /api/coverage, GET /api/billing vs GET /api/crawl/runs (run counter names)
   problem: The same two counters have different names in three endpoints: /api/crawl/runs returns n_rows/n_new (documented skill/references/api.md:19), /api/coverage renames them inside last_run to rows/new (app/coverage.py:103), /api/billing does the same in runs[] (app/billing.py:318-321). web/pro.template.html:987 maps back with n_rows:r.rows, n_new:r.new. `rows` is the pagination array key everywhere else in this API, so an integer row-count under that name is actively misleading.
   fix: Emit n_rows/n_new in app/coverage.py:103 and app/billing.py:318-321 and delete the remapping at web/pro.template.html:987; emit both names for one release if breaking those responses is a concern.

27. [low] POST /api/crawl vs GET /api/crawl/plan (response key types)
   problem: POST /api/crawl returns "adapter"/"firecrawl" as integers (app/main.py:224-225) while GET /api/crawl/plan returns via_adapter/via_firecrawl for the counts AND "adapter"/"firecrawl" as lists of clinic_ids (app/main.py:237-241) -- so typeof res.adapter flips between number and object across the preview-then-fire flow the /pro page does at web/pro.template.html:728. The plan response also ships est_credits and credits_needed with identical values.
   fix: Rename the id lists to adapter_clinic_ids / firecrawl_clinic_ids, drop est_credits (keep credits_needed, the name POST /api/crawl already uses), and rename the POST counts to via_adapter/via_firecrawl so both endpoints speak one vocabulary.

28. [low] GET /api/crawl/plan vs POST /api/crawl (max_credits handling)
   problem: max_credits defaults to a hardcoded 40 on the plan endpoint (app/main.py:229) but to ST.get_firecrawl()['default_max_credits'] on POST (app/main.py:208), and the plan applies none of POST's 0..500 validation -- so raising default_max_credits to 120 makes the preview understate 3x for any caller that omits the param (skill/references/api.md does not list max_credits for /crawl/plan at all), max_credits=600 previews happily then 400s, and a negative value previews a negative estimate. In the other direction POST accepts anything in 0..500 while spend_gate refuses every clinic whose effective cap is below MIN_VIABLE_CAP=40 (app/crawl.py:34), so max_credits=20 is accepted, queued, and refuses every clinic with 'budget too thin' -- the exact failure the floor was added to prevent, moved one layer up.
   fix: Share one helper for default+bounds between the two handlers, reject `0 < max_credits < CR.MIN_VIABLE_CAP` for non-adapter modes at the boundary, mirror the floor as the min of the credits input in web/pro.template.html, and expose MIN_VIABLE_CAP plus est_usd (est_credits * eur_per_credit) in the plan response.

29. [low] GET /api/schedules/presets (unused) + GET /api/schedules (human field)
   problem: The endpoint exists (app/main.py:302, returns SC.PRESETS) and neither template calls it. web/pro.template.html:684 hardcodes a copy of app/schedules.py:15-21 (even renaming stagger_days -> stagger) and l.828 hardcodes the option list. Adding or retuning a preset server-side shows the user a cron that is not what will run (create/update overwrites it from the server PRESETS). Related dead weight: cronHuman() (l.686-689, with its own DE/EN day-name map) only fires as a fallback for s.human, which app/schedules.py:52 sets on every row.
   fix: Fetch GET /api/schedules/presets in scheduleDialog and build both the option list and the cron/stagger sync from it (keys = options, custom = {}). Delete the PRESETS literal and cronHuman().

30. [low] GET /api/billing?window=today + boot chain (web/pro.template.html:981, :1168; web/index.template.html:190)
   problem: Two cheap client-side wins. billToday() fetches the full billing report -- series[], by_kind[], runs[] capped at RUNS_CAP (app/billing.py:323), plus the whole run/ledger join -- to render one header float (d.totals.usd), at boot and again from every kostenLink() render on the Clawl page. And boot chains four independent round trips strictly sequentially: gate().then(taxonomy().then(loadStats).then(route)), with the same pattern inside pages (index.template.html:227/:249/:336, pro.template.html:545-553/:701).
   fix: Add today_usd to GET /api/stats (already polled every 60s and already the source of the other header numbers) and delete billToday() -- removes a request rather than shrinking one. Replace the boot chain with Promise.all([taxonomy(), loadStats()]).then(route) and each `await A; await B` pair with Promise.all.

31. [low] GET /api/schedules (next_run_at), GET /api/stats (next_autocrawl), GET /api/billing (pools.period_*)
   problem: next_run uses timespec="minutes" (app/schedules.py:130) -> '2026-09-09T03:00+00:00' while every other timestamp uses timespec="seconds" (app/runs.py:55), and that value surfaces on /api/schedules rows and as /api/stats.next_autocrawl. GET /api/billing mixes notations in one body: window.from/to are +00:00 (app/billing.py:338) while pools.period_start/end pass Firecrawl's '...000Z' through verbatim (app/billing.py:335-336). The codebase itself compares timestamps as strings (app/schedules.py:207, app/coverage.py:288), where '...T03:00+00:00' < '...T03:00:00+00:00' lexically; Date.parse hides it in the SPA but not for agent consumers.
   fix: timespec="seconds" at app/schedules.py:130 and app/scheduler.py:38; normalise the Firecrawl period fields through the existing _bc_parse/_iso pair at app/billing.py:335-336; state one format (UTC, second precision, +00:00) in docs/api.md.

32. [low] GET /api/autopilot/interviews?from=&to=, GET /api/autopilot/queue?due_before=
   problem: Autopilot stores Europe/Berlin-offset timestamps (TZ at app/autopilot/seed.py:23, S.iso() -> '...+02:00') while the rest of the API is UTC +00:00. The filters bind the raw query string into SQL and compare lexicographically with no normalisation (app/autopilot/api.py:710-715 `i.at>=?`, :822-824 `due_at<=?`), so a caller sending a UTC bound gets a byte comparison, not a time comparison -- silently wrong result sets, no error. Today only the autopilot SPA calls these, which is why it has not bitten yet.
   fix: Run the bound through the existing S.parse() then S.iso() before it reaches SQL in both places. Longer term store autopilot timestamps in UTC like every other module and convert to Berlin for display only.

33. [low] GET /api/openapi.json
   problem: The generated schema is weaker than the docs and disagrees with them: /api/clinics, /api/jobs, /api/plan declare zero query parameters (handlers take a raw Request), so none of the documented filters appear; POST /api/crawl, POST/PUT /api/schedules, PUT /api/settings/patterns, POST /api/campaign and most autopilot POSTs have no requestBody schema (bodies read via await request.json()); every 200 response schema is empty; there are no securitySchemes, so the 8 owner-gated read prefixes look public.
   fix: Cheapest: state in docs/api.md + skill/references/api.md that /api/openapi.json is a route index only and the field/filter contract lives in the docs. Only if agents actually consume it: typed Query params for /api/clinics and /api/jobs and a Pydantic body model for POST /api/crawl.

34. [low] GET /api/postings/{posting_id}/closed (app/stripe_gate.py:383)
   problem: Closed postings are readable one id at a time; there is no list over closed_postings(posting_id, by_email, at, stripe_customer_id, stripe_usage_id, stripe_error). GET /api/stripe/status returns only a closed_this_period count and /api/billing covers Firecrawl spend, not revenue. The per-row stripe_error written at app/stripe_gate.py:378 ('stripe not configured', 'stripe unreachable: ...', 'no stripe customer for this address') is therefore visible to nobody without opening data/app.sqlite on the host -- a usage unit that never reached Stripe is silently lost revenue.
   fix: Add GET /api/stripe/closed?window=&limit= (owner sees all, a customer only their own by_email) -- one `select * from closed_postings where at>=? order by at desc` next to the existing helpers, rows enriched with title/clinic and surfacing billed/stripe_error.

